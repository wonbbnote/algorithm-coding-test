




SELECT a.SCORE, h.EMP_NO, h.EMP_NAME, h.POSITION, h.EMAIL
FROM
(
SELECT EMP_NO, SUM(SCORE) AS SCORE
FROM HR_GRADE
GROUP BY EMP_NO
) AS a
JOIN HR_EMPLOYEES h ON a.EMP_NO = h.EMP_NO
ORDER BY SCORE DESC
LIMIT 1;

